# lib-only repository view: keep only lib/ in VCS
# Ignore everything by default
/*
# Keep .gitignore itself
!.gitignore
# Keep pubspec for dependencies
!pubspec.yaml
# Keep lib sources
!lib/
# Keep documentation and screenshots
!README.md
!HELP.md
!CHANGELOG.md
!docs/
# Do not track local experimental folders
# Dogs/ — локальная папка, исключаем из репозитория
# Чтобы оставить файлы локально, используйте git rm --cached
# (см. коммит в данной ветке)
#
# Исключаем Dogs из списка сохраняемых
# (по умолчанию /* уже игнорирует всё, а ! снимает игнор)
# Просто убираем правило !Dogs/
!ios/Runner/Info.plist
!ios/Runner.xcodeproj/project.pbxproj

# Explicitly ignore local VS Code settings
/.vscode/
