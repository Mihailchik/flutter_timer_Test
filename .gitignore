# lib-only repository view: keep only lib/ in VCS
# Ignore everything by default
/*
# Keep .gitignore itself
!.gitignore
# Keep pubspec for dependencies
!pubspec.yaml
# Keep lib sources
!lib/
# Keep documentation and screenshots
!README.md
!HELP.md
!CHANGELOG.md
!docs/
# Ignore local project rules file (kept out of VCS)
PROJECT_RULES.md
